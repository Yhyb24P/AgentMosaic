.venv/
.pytest_cache/
.mypy_cache/
__pycache__/
# Rust build output.
/target/
# Documentation trees are tracked: docs/ (current product docs), including
# docs/runtimes/ and docs/releases/ (public release notes).
*.py[cod]
release-manifest.json
*-evidence.json
*-evidence-check.json
# Runtime state and SQLite sidecars must never enter the release baseline.
*.db
*.db-wal
*.db-shm
/backup/
/backups/
